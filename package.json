{
  "name": "plug-service-v2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon index",
    "migrate:up": "npx sequelize-cli db:migrate",
    "migrate:undo": "npx sequelize-cli db:migrate:undo",
    "create:migrate": "npx sequelize-cli  migration:create --migrations-path migrations --name",
    "migrateT:up": "sequelize-cli db:migrate --config config/timescale_config.js --migrations-path migrations/timescale",
    "migrateT:undo": "sequelize-cli db:migrate:undo --config config/timescale_config.js --migrations-path migrations/timescale",
    "create:migrateT": "npx sequelize-cli  migration:create --migrations-path migrations/timescale --name",
    "create:migrateP": "npx sequelize-cli  migration:create --migrations-path migrations --name",
    "dockerStart": "sequelize db:migrate && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@elastic/elasticsearch": "^8.10.0",
    "@google/generative-ai": "^0.2.1",
    "axios": "^1.6.3",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "joi": "^17.12.3",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.3.0",
    "mongoose": "^8.1.1",
    "mysql2": "^3.6.5",
    "nodemon": "^3.0.1",
    "openai": "^4.26.1",
    "pg": "^8.11.3",
    "rtlayer-node": "^1.0.2",
    "sequelize": "^6.35.2"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.2"
  }
}
